<view class="tab">
    <block wx:for="{{tabs}}" wx:key="id">
        <view class="tab-item {{item.isActive? 'active': ''}}" bind:tap="clickTap" data-id="{{item.id}}">{{item.name}}</view>
    </block>
</view>